import { IonFooter, IonTitle, IonToolbar } from '@ionic/react';
import React from 'react';

const Footer: React.FC = () => (
  <IonFooter>
    <IonToolbar>
      <IonTitle>共通フッター</IonTitle>
    </IonToolbar>
  </IonFooter>
);

export default Footer;
